FUNCTIONS IN JS
    
    CalculateArea
        get window height 
        get window width
        calculate area
        store area
        
    CreateMessage
        do calculation
        check value
        output message
    
    animateImage
        get image position
        calculate new value
        set image position
        change text color
        if position correct
        output message
        
    scope
        var foo = 500;

            function myFunc(){

              foo = 600;
            }

            myFunc();
            console.log(foo);